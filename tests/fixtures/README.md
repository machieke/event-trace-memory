# Test Fixtures

`minimum-corpus-v0.1.json` is the reusable fixture corpus for the minimum
objects listed in section 22.3 of the implementation plan. It is intentionally
data-only: tests load it and execute the reference APIs so fixtures stay aligned
with the implementation instead of becoming static examples that drift.

